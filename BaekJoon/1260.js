const readline = require('readline');
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});
let input = []; //전역변수로 사용하는 것들 let graph = []; let visited = []; let visited2 = []; let DFS_graph = []; let BFS_graph = [];
